import { describe, it } from 'vitest'

describe('webauthn expiry behavior (t-fn-015 / tc-fn-008)', () => {
  it.todo('reuses valid credential in effective window and skips re-verification')
  it.todo('prompts verification again when credential is expired')
  it.todo('shows graceful fallback when WebAuthn is not supported')
})
