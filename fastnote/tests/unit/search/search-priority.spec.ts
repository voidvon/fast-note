import { describe, it } from 'vitest'

describe('search priority (t-fn-010 / tc-fn-007)', () => {
  it.todo('ranks title matches before content matches')
  it.todo('returns readable snippet/highlight for matched content')
  it.todo('applies debounce to avoid repeated search calls within 300ms')
})
