import { describe, it } from 'vitest'

describe('desktop redirect route guard (t-fn-009 / tc-fn-006)', () => {
  it.todo('redirects /n/:id to /home on desktop width >= 640')
  it.todo('redirects /f/:path to /home on desktop width >= 640')
  it.todo('keeps route unchanged on mobile width < 640')
})
