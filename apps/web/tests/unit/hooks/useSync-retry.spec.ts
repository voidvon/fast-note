import { describe, it } from 'vitest'

describe('t-FN-003 / TC-FN-003 useSync retry and stats', () => {
  it.todo('retries failed sync items with bounded attempts')
  it.todo('returns aggregated stats: uploaded/downloaded/failed')
  it.todo('advances cursor only for successfully synchronized changes')
})
