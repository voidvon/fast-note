# 组件与数据契约清单（Fast-Note）

## 1. 文档信息

- 输出日期：2026-03-04
- 输出文件：`doc/开发计划/组件与数据契约清单.md`
- 输入来源：
  - `doc/功能拆分与技术方案/Fast-Note-功能拆分与技术方案-总览-2026-03-04.md`
  - `doc/功能拆分与技术方案/Fast-Note-功能拆分与技术方案-*.md`

## 2. 组件清单（UI 层）

| 组件ID   | 组件路径                                       | 关联功能点       | 输入 Props                                     | 输出 Events              | 状态依赖                                       | 备注                   |
| -------- | ---------------------------------------------- | ---------------- | ---------------------------------------------- | ------------------------ | ---------------------------------------------- | ---------------------- |
| C-FN-001 | `src/views/UserPublicNotesPage.vue`            | F4.2, F4.3       | `username(route param)`、`isDesktop`、`noteId` | 无（内部路由驱动）       | `useUserPublicNotes`, `useUserPublicNotesSync` | 需修复 `note-id` 传参  |
| C-FN-002 | `src/views/NoteDetail.vue`                     | F2.2, F5.1, F5.2 | `noteId`, `parentId`                           | `noteSaved`              | `useNote`, `useSync`, `useWebAuthn`            | 编辑主入口             |
| C-FN-003 | `src/components/GlobalSearch/GlobalSearch.vue` | F1.3             | `puuid`                                        | 无                       | `useNote.searchNotesByParentId`                | 300ms 防抖             |
| C-FN-004 | `src/components/UserProfile.vue`               | F3.3             | 无                                             | 无                       | `authManager`, `useSync`                       | 手动同步入口与状态展示 |
| C-FN-005 | `src/components/NoteMore.vue`                  | F2.3, F5.2       | `isOpen`                                       | `update:isOpen`          | `useNote`, `useWebAuthn`                       | 分享、锁定、删除       |
| C-FN-006 | `src/components/LongPressMenu.vue`             | F2.3             | `id`, `items`                                  | `refresh`, `move`        | `useNote`                                      | 需补深层递归删除       |
| C-FN-007 | `src/components/NoteMove.vue`                  | F2.1             | `id`, `isOpen`                                 | `refresh`                | `useNote`                                      | 移动后更新父级计数     |
| C-FN-008 | `src/views/FolderPage.vue`                     | F2.1, F2.3       | `currentFolder`, `selectedNoteId`              | `selected`, `createNote` | `useNote`                                      | 文件夹与笔记列表混排   |
| C-FN-009 | `src/views/HomePage.vue`                       | F1.2, F2.1       | 无                                             | 无                       | `useDeviceType`, `useNote`                     | 桌面分栏入口           |
| C-FN-010 | `src/views/DeletedPage.vue`                    | F2.3             | 无                                             | `selected`               | `useNote.getDeletedNotes`                      | 回收站页面             |

## 3. 编排层契约（Hooks）

| HookID   | Hook 路径                             | 关联功能点             | 输入参数                  | 返回值                                 | 副作用                                          | 错误处理                         |
| -------- | ------------------------------------- | ---------------------- | ------------------------- | -------------------------------------- | ----------------------------------------------- | -------------------------------- |
| H-FN-001 | `src/hooks/useSync.ts`                | F3.1, F3.2, F3.3, F5.1 | `silent:boolean`          | `syncResult`, `syncStatus`, `onSynced` | 读写 `localStorage.pocketbaseUpdated`、远程同步 | 当前单条失败即中断，需补失败分级 |
| H-FN-002 | `src/hooks/useUserPublicNotesSync.ts` | F1.1, F4.2             | `username`                | `syncUserPublicNotes`                  | 拉取公开用户与公开笔记并写 store                | 失败抛错由调用方处理             |
| H-FN-003 | `src/hooks/useNavigationHistory.ts`   | F1.2                   | Router                    | `getSmartBackPath` 等                  | 持久化到 `localStorage`                         | 存储异常静默                     |
| H-FN-004 | `src/hooks/useLastVisitedRoute.ts`    | F1.3                   | Router                    | `restoreLastVisitedRoute` 等           | 保存最后访问路由                                | 登录/注册排除                    |
| H-FN-005 | `src/hooks/useEditor.ts`              | F5.1                   | FileList                  | `insertFiles`, `extractFileHashes`     | 本地 hash 计算与文件引用注入                    | 文件处理异常记日志               |
| H-FN-006 | `src/hooks/useWebAuthn.ts`            | F5.2                   | `force:boolean`（verify） | `register`, `verify`, `state`          | 本地存储 credential/timestamp                   | 不支持时提示并返回 false         |
| H-FN-007 | `src/hooks/useDeviceType.ts`          | F1.2                   | 无                        | `isDesktop`                            | 全局监听 resize                                 | 需评估长时性能影响               |

## 4. 领域层契约（Stores）

| StoreID  | Store 路径                  | 读接口                                                                      | 写接口                                                              | 状态源                                        | 一致性规则                                      |
| -------- | --------------------------- | --------------------------------------------------------------------------- | ------------------------------------------------------------------- | --------------------------------------------- | ----------------------------------------------- |
| S-FN-001 | `src/stores/notes.ts`       | `getNote`, `getNotesByParentId`, `getDeletedNotes`, `searchNotesByParentId` | `addNote`, `updateNote`, `deleteNote`, `updateParentFolderSubcount` | `notes`（内存） + Dexie 同步                  | `updated` 时间推进，`note_count` 需与树结构一致 |
| S-FN-002 | `src/stores/publicNotes.ts` | `getPublicNote`, `getPublicFolderTreeByPUuid`                               | `addPublicNote`, `updatePublicNote`, `deletePublicNote`             | `UserPublicNotes_{username}` Dexie + 内存 ref | 按 username 隔离，不可污染私有数据              |

## 5. 数据层契约（Database / PocketBase）

### 5.1 本地数据库契约（Dexie）

| 表名       | 字段                                                                                                   | 索引                                                    | 变更类型（新增/修改/删除）        | 兼容策略                                      |
| ---------- | ------------------------------------------------------------------------------------------------------ | ------------------------------------------------------- | --------------------------------- | --------------------------------------------- |
| notes      | `id,title,summary,content,item_type,parent_id,updated,is_deleted,is_locked,is_public,note_count,files` | `&id`, `[item_type+parent_id+is_deleted]`, `updated` 等 | 计划新增：无（保持兼容）          | 继续使用字符串 `id`，时间字段统一 `getTime()` |
| note_files | `hash,file,fileName,fileSize,fileType,created,updated`                                                 | `&hash`                                                 | 计划新增：`[待确认] upload_state` | 若新增状态字段，默认值向后兼容                |
| user_info  | `id,username,name`                                                                                     | `&id`                                                   | 无                                | 无                                            |
| metadata   | `key,value`                                                                                            | `&key`                                                  | 无                                | 无                                            |

### 5.2 云端契约（PocketBase）

| Collection   | 字段                                                       | 过滤规则                                      | 写入策略                                  | 冲突策略                             |
| ------------ | ---------------------------------------------------------- | --------------------------------------------- | ----------------------------------------- | ------------------------------------ |
| notes        | `id,user_id,updated,is_deleted,is_public,files,content...` | 私有同步：`user_id=auth.id && updated>cursor` | `updateNote` upsert（create/update 回退） | 比较 `updated`，新者覆盖旧者         |
| users        | `email,username,password...`                               | 认证接口使用                                  | 注册后自动登录                            | 认证失败清理 authStore               |
| public_users | `username,id,...`                                          | `username = "..."`                            | 只读查询                                  | 无写回                               |
| files        | `hash,path,type,size...`                                   | 按 hash 查询                                  | 文件上传后 URL 回填                       | 与 note files 引用一致性依赖内容替换 |

## 6. 路由契约（Router）

| 路由                           | 进入条件             | 参数                 | 权限                 | 失败回退                    |
| ------------------------------ | -------------------- | -------------------- | -------------------- | --------------------------- |
| `/home`                        | 应用启动或桌面重定向 | 无                   | 登录可选（离线可用） | 初始化失败仍挂载应用        |
| `/n/:id`                       | 选择私有笔记         | `id`                 | 私有上下文可编辑     | 桌面端重定向到 `/home`      |
| `/f/:pathMatch(.*)*`           | 进入私有文件夹       | `pathMatch`          | 私有上下文可编辑     | 桌面端重定向到 `/home`      |
| `/:username`                   | 访问公开主页         | `username`           | 无需登录             | 初始化失败显示错误态 + 重试 |
| `/:username/f/:pathMatch(.*)*` | 访问公开文件夹       | `username,pathMatch` | 无需登录             | 回退公开主页                |
| `/:username/n/:noteId`         | 访问公开笔记         | `username,noteId`    | 无需登录（只读）     | 回退公开文件夹/主页         |
| `/deleted`                     | 进入回收站           | 无                   | 私有上下文           | 回退 `/home`                |
| `/login`, `/register`          | 认证入口             | 无                   | 未登录访问           | 登录后回上一路径            |

## 7. 跨层影响矩阵

| 功能点ID | UI                           | Hooks              | Stores   | Database                         | PocketBase          | Router                  |
| -------- | ---------------------------- | ------------------ | -------- | -------------------------------- | ------------------- | ----------------------- |
| F1.1     | C-FN-001                     | H-FN-002           | S-FN-002 | UserPublicNotes DB               | public_users, notes | `/:username*`           |
| F1.2     | C-FN-009                     | H-FN-003, H-FN-007 | S-FN-001 | notes                            | 无                  | `/n/*`, `/f/*`, `/home` |
| F1.3     | C-FN-003                     | H-FN-004           | S-FN-001 | notes                            | 无                  | 全局                    |
| F2.1     | C-FN-007, C-FN-008           | 无                 | S-FN-001 | notes                            | notes               | `/f/*`                  |
| F2.2     | C-FN-002                     | 无                 | S-FN-001 | notes                            | notes               | `/n/:id`                |
| F2.3     | C-FN-005, C-FN-006, C-FN-010 | 无                 | S-FN-001 | notes                            | notes               | `/deleted`              |
| F3.1     | C-FN-004                     | H-FN-001           | S-FN-001 | notes, metadata                  | notes               | 无                      |
| F3.2     | C-FN-010                     | H-FN-001           | S-FN-001 | notes                            | notes               | `/deleted`              |
| F3.3     | C-FN-004                     | H-FN-001           | S-FN-001 | notes                            | notes               | 无                      |
| F4.1     | C-FN-001                     | 无                 | 无       | 无                               | users               | `/login`, `/register`   |
| F4.2     | C-FN-001                     | H-FN-002           | S-FN-002 | UserPublicNotes DB               | public_users, notes | `/:username*`           |
| F4.3     | C-FN-001, C-FN-002           | 无                 | S-FN-002 | UserPublicNotes DB               | notes               | `/:username/n/:noteId`  |
| F5.1     | C-FN-002                     | H-FN-001, H-FN-005 | S-FN-001 | note_files, notes                | notes, files        | `/n/:id`                |
| F5.2     | C-FN-002, C-FN-005           | H-FN-006           | S-FN-001 | notes + localStorage(credential) | notes               | `/n/:id`                |

## 8. 2026-03-06 需求增补：PC 模式刷新恢复当前备忘录

### 8.1 增补组件/Hook 契约

| 契约ID   | 路径                                | 关联功能点 | 输入       | 输出                                           | 状态源/副作用                                        | 备注                                 |
| -------- | ----------------------------------- | ---------- | ---------- | ---------------------------------------------- | ---------------------------------------------------- | ------------------------------------ |
| C-FN-011 | `src/views/HomePage.vue`            | F1.4       | 无         | 无                                             | `useDesktopActiveNote`, `useNote`, `useDeviceType`   | 恢复并驱动 `folerId/noteId/parentId` |
| H-FN-008 | `src/hooks/useDesktopActiveNote.ts` | F1.4       | `snapshot` | `getSnapshot`, `saveSnapshot`, `clearSnapshot` | 读写 `localStorage.flashnote_desktop_active_note_v1` | JSON 异常需静默清理                  |

### 8.2 增补本地状态契约

| 契约名称                    | 介质           | 字段                                        | 写入时机                             | 读取时机                  | 失败处理                   |
| --------------------------- | -------------- | ------------------------------------------- | ------------------------------------ | ------------------------- | -------------------------- |
| `DesktopActiveNoteSnapshot` | `localStorage` | `folderId`, `noteId`, `parentId`, `savedAt` | 桌面端切换目录、切换笔记、新建成功后 | `HomePage` 桌面初始化阶段 | 解析失败清理并回退默认逻辑 |

### 8.3 增补路由/页面规则

| 规则ID   | 规则说明                                                  | 触发位置                            | 兼容要求               |
| -------- | --------------------------------------------------------- | ----------------------------------- | ---------------------- |
| R-FN-001 | 桌面端仍保持 `/n/*`、`/f/*` 刷新后重定向 `/home`          | `src/router/index.ts`               | 不改变现有桌面路由语义 |
| R-FN-002 | 桌面端 `/home` 初始化优先恢复快照，再执行默认首条笔记兜底 | `src/views/HomePage.vue`            | 移动端不生效           |
| R-FN-003 | 快照中的 `noteId='0'` 或空值不参与恢复                    | `src/hooks/useDesktopActiveNote.ts` | 避免恢复到未保存新建态 |

### 8.4 跨层影响补充

| 功能点ID | UI       | Hooks              | Stores   | Database   | PocketBase | Router  |
| -------- | -------- | ------------------ | -------- | ---------- | ---------- | ------- |
| F1.4     | C-FN-011 | H-FN-008, H-FN-007 | S-FN-001 | 无结构变更 | 无         | `/home` |

## 9. 2026-03-06 需求增补：PC 空白详情点击新建备忘录

### 9.1 增补组件契约

| 契约ID   | 路径                       | 关联功能点 | 输入                     | 输出                              | 状态源/副作用                                                        | 备注                               |
| -------- | -------------------------- | ---------- | ------------------------ | --------------------------------- | -------------------------------------------------------------------- | ---------------------------------- |
| C-FN-012 | `src/views/HomePage.vue`   | F2.4       | 无                       | 无（内部调用 `handleCreateNote`） | `useDeviceType`, 本地 `state.noteId/state.parentId`, `clearSnapshot` | 在桌面端无选中笔记时渲染空态创建层 |
| C-FN-013 | `src/views/NoteDetail.vue` | F2.4       | `noteId='0'`, `parentId` | `noteSaved`                       | `editorRef.focus()`, `useNote`, `useSync`                            | 复用现有新建态与正文聚焦逻辑       |

### 9.2 增补页面交互契约

| 规则ID   | 规则说明                                                          | 触发位置                   | 兼容要求                           |
| -------- | ----------------------------------------------------------------- | -------------------------- | ---------------------------------- |
| R-FN-004 | 仅在桌面端且 `state.noteId === ''` 时显示空白详情点击层           | `src/views/HomePage.vue`   | 移动端不生效                       |
| R-FN-005 | 点击空白详情层直接复用 `handleCreateNote()`，不新增第二套新建逻辑 | `src/views/HomePage.vue`   | 与底部“新建备忘录”按钮结果一致     |
| R-FN-006 | 进入 `noteId='0'` 的新建态后，空态点击层立即移除，防止重复触发    | `src/views/HomePage.vue`   | 不生成多个并发新建会话             |
| R-FN-007 | 富文本正文聚焦继续由 `NoteDetail` 新建态逻辑负责                  | `src/views/NoteDetail.vue` | 不在 `HomePage` 手动操作编辑器实例 |

### 9.3 跨层影响补充

| 功能点ID | UI                 | Hooks              | Stores   | Database   | PocketBase | Router  |
| -------- | ------------------ | ------------------ | -------- | ---------- | ---------- | ------- |
| F2.4     | C-FN-012, C-FN-013 | H-FN-007, H-FN-008 | S-FN-001 | 无结构变更 | 无         | `/home` |

### 9.4 数据与状态说明

| 契约名称                     | 状态归属                          | 字段/值          | 写入时机 | 读取时机                    | 失败处理                                     |
| ---------------------------- | --------------------------------- | ---------------- | -------- | --------------------------- | -------------------------------------------- | --------------------------------------- | --------------------------------- |
| `HomePageDesktopDetailState` | `src/views/HomePage.vue` 本地状态 | `state.noteId='' | '0'      | <noteId>`, `state.parentId` | 切换目录、点击空态创建、选择笔记、新建保存后 | 右侧详情区渲染分支与 `NoteDetail` props | 异常时回退现有详情空态/编辑态逻辑 |

### 9.5 测试文件规划映射

| 功能点ID | 组件/页面                                             | 测试文件                                                     | 关注点                                           |
| -------- | ----------------------------------------------------- | ------------------------------------------------------------ | ------------------------------------------------ |
| F2.4     | `src/views/HomePage.vue`                              | `tests/unit/views/home-empty-detail-create.spec.ts`          | 空态显示条件、点击创建、防重复                   |
| F2.4     | `src/views/HomePage.vue` + `src/views/NoteDetail.vue` | `tests/integration/home/desktop-empty-detail-create.spec.ts` | 右侧空态点击进入新建、正文聚焦、已有详情不误创建 |

## 10. 2026-03-08 需求增补：编辑反馈与输入聚焦

### 10.1 增补组件契约（UI 层）

| 契约ID   | 路径                       | 关联功能点 | 输入                              | 输出                                   | 状态源/副作用                                                         | 备注                                    |
| -------- | -------------------------- | ---------- | --------------------------------- | -------------------------------------- | --------------------------------------------------------------------- | --------------------------------------- |
| C-FN-014 | `src/views/HomePage.vue`   | F2.5       | 无                                | 无（通过 `addButtons` 写入 `addNote`） | `focusFolderAlertInput`, `IonAlert didPresent`, `keyboardClose=false` | 负责首页“新建文件夹”弹窗聚焦兜底        |
| C-FN-015 | `src/views/FolderPage.vue` | F2.5       | `currentFolder`, `selectedNoteId` | `selected`, `createNote`               | `showAddFolderAlert`, `focusFolderAlertInput`                         | 负责文件夹页“新建文件夹”弹窗聚焦兜底    |
| C-FN-016 | `src/views/NoteDetail.vue` | F2.6       | `noteId`, `parentId`              | `noteSaved`                            | `state.isSaving`, `presentTopError`, `useSync`, `useNote`             | 负责详情保存状态 spinner 与顶部错误提示 |

### 10.2 增补页面交互契约

| 规则ID   | 规则说明                                                                                                       | 触发位置                                             | 兼容要求                                               |
| -------- | -------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------- | ------------------------------------------------------ |
| R-FN-008 | 首页与文件夹页的新建文件夹弹窗在 `didPresent` 后二次 `focus()` 输入框，并关闭 `keyboardClose` 的默认收键盘行为 | `src/views/HomePage.vue`, `src/views/FolderPage.vue` | 不改文件夹创建数据流，仅影响表现层焦点体验             |
| R-FN-009 | `NoteDetail` 仅对非静默保存显示导航栏 spinner，`silent=true` 场景保持无感知保存                                | `src/views/NoteDetail.vue`                           | 桌面切换笔记时不出现频繁 loading 闪动                  |
| R-FN-010 | 本地保存失败与同步失败统一通过顶部 danger toast 提示，不再展示底部成功 toast                                   | `src/views/NoteDetail.vue`                           | 继续保留本地优先保存语义，不影响 Dexie/PocketBase 契约 |

### 10.3 增补 Hook / Store / 数据层说明

| 层级       | 路径                    | 关联功能点 | 输入/输出                             | 变化说明                                         | 备注                                            |
| ---------- | ----------------------- | ---------- | ------------------------------------- | ------------------------------------------------ | ----------------------------------------------- |
| Hooks      | `src/hooks/useSync.ts`  | F2.6       | `sync(true)`                          | 无接口变更；继续由页面消费同步异常并转为顶部提示 | 保持 `silent=true` 未登录返回 `null` 的既有语义 |
| Stores     | `src/stores/notes.ts`   | F2.5, F2.6 | `addNote`, `updateNote`, `deleteNote` | 无接口变更                                       | 文件夹创建与笔记保存仍通过 store 执行           |
| Database   | `src/database/dexie.ts` | F2.5, F2.6 | 无                                    | 无结构变更                                       | 不新增 schema/index                             |
| PocketBase | `src/pocketbase/*.ts`   | F2.6       | 无                                    | 无结构变更                                       | 同步失败仅调整页面展示，不改变云端契约          |
| Router     | `src/router/index.ts`   | F2.5, F2.6 | 无                                    | 无结构变更                                       | 路由语义不受影响                                |

### 10.4 跨层影响补充

| 功能点ID | UI                 | Hooks    | Stores   | Database   | PocketBase             | Router   |
| -------- | ------------------ | -------- | -------- | ---------- | ---------------------- | -------- |
| F2.5     | C-FN-014, C-FN-015 | 无       | S-FN-001 | 无结构变更 | 无                     | 无       |
| F2.6     | C-FN-016           | H-FN-001 | S-FN-001 | 无结构变更 | notes 同步契约保持不变 | `/n/:id` |

### 10.5 数据与状态补充

| 契约名称                      | 状态归属                            | 字段/值                         | 写入时机                                        | 读取时机                    | 失败处理                                         |
| ----------------------------- | ----------------------------------- | ------------------------------- | ----------------------------------------------- | --------------------------- | ------------------------------------------------ |
| `FolderAlertFocusState`       | 页面临时 DOM 状态                   | `alert element`, `.alert-input` | `IonAlert didPresent`                           | 弹窗输入框挂载完成后        | 找不到输入框则静默返回，保留用户手动点击         |
| `NoteDetailSaveFeedbackState` | `src/views/NoteDetail.vue` 本地状态 | `state.isSaving:boolean`        | 非静默保存开始时置 `true`，`finally` 置 `false` | 导航栏右侧 spinner 渲染分支 | 保存异常时仍在 `finally` 收口，避免 spinner 残留 |
