import { describe, it } from 'vitest'

describe('t-FN-002 / TC-FN-002 notes subtree delete', () => {
  it.todo('deletes a folder and marks all descendants as is_deleted = 1')
  it.todo('keeps recycle bin query results consistent after subtree delete')
})
