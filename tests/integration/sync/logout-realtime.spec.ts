import { describe, it } from 'vitest'

describe('logout realtime and manual sync behavior (tc-fn-009)', () => {
  it.todo('disconnects realtime subscription after logout')
  it.todo('shows not-logged-in prompt when manual sync is triggered after logout')
})
