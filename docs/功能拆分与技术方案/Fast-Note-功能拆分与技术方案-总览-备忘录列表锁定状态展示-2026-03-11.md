# fastnote 功能拆分与技术方案（总览）- 备忘录列表锁定状态展示

## 1. 文档信息

- 版本/迭代：备忘录列表锁定状态展示方案 V0.1
- 输出日期：2026-03-11
- 本次对齐日期：2026-03-18
- 输出文件：`docs/功能拆分与技术方案/fastnote-功能拆分与技术方案-总览-备忘录列表锁定状态展示-2026-03-11.md`
- 输入来源：
  - 当前会话确认的《备忘录列表锁定状态展示 PRD》
  - 当前仓库代码
- 输出范围：
  - 列表项左侧锁状态图标展示
  - 未锁定项固定占位与标题对齐
  - 深色模式适配与图标尺寸复用策略
  - 最小回归验证建议
- 不在范围：
  - 锁定/解锁业务逻辑改造
  - `Note` 契约、Dexie schema、PocketBase 字段调整
  - 详情页解锁面板与锁管理弹层调整

## 2. 功能拆分总览

| 能力域 | 功能点 | 用户问题/价值 | 优先级 | 当前状态（已支持/部分支持/未支持） | 关键证据 |
| ------ | ------ | ------------- | ------ | ---------------------------------- | -------- |
| 备忘录列表锁状态展示 | F1.1 列表项锁图标与占位布局 | 用户在列表页直接识别已锁备忘录，同时保证所有标题起始位置一致 | P0 | 已支持 | `src/widgets/note-list/ui/note-list-item.vue:49`, `src/widgets/note-list/ui/note-list-item.vue:160` |
| 备忘录列表锁状态展示 | F1.2 深色模式与视觉复用 | 锁图标在浅色/深色主题下均清晰可见，尺寸与现有列表前导图标保持统一 | P0 | 已支持 | `src/widgets/note-list/ui/note-list-item.vue:267`, `src/css/var.scss:47` |
| 备忘录列表锁状态展示 | F1.3 列表展示回归验证 | 保证锁定状态、占位和多场景列表显示不回退 | P1 | 已支持 | `tests/unit/components/note-list-item-lock-indicator.spec.ts:75`, `tests/integration/home/note-list-lock-indicator.spec.ts:137` |

## 3. 模块文档索引

| 模块 | 文档文件 | 功能点数量 | 负责人建议 | 备注 |
| ---- | -------- | ---------- | ---------- | ---- |
| 备忘录列表锁定状态展示 | `docs/功能拆分与技术方案/fastnote-功能拆分与技术方案-备忘录列表锁定状态展示-2026-03-11.md` | 3 | 前端 / UI | 基础能力已落地，并被解锁态三态实现继续复用 |

## 4. 里程碑与发布建议

- 里程碑状态：
  - M1：`NoteListItem` note 行左侧状态槽位与占位对齐，已完成。
  - M2：主题色和尺寸样式复用，已完成。
  - M3：组件/集成层最小验证，已完成。
- 上线策略：
  - 属于列表展示层增强，不涉及数据迁移或协议变更。
- 回滚边界：
  - 若后续出现标题错位或主题可读性问题，可只回退 `src/widgets/note-list/ui/note-list-item.vue` 的前导槽位与样式。

## 5. 跨模块依赖与风险

- 依赖关系：
  - 依赖 `src/shared/types/index.ts` 对锁字段的标准化。
  - 依赖 `src/widgets/note-list/ui/note-list.vue` 全量复用 `NoteListItem` 渲染入口。
  - 依赖 `src/css/var.scss` 现有 token 和 `ion-palette-dark` 主题切换机制。
- 当前风险：
  - 当前集成测试主覆盖首页列表，文件夹页仍以共享 widget 复用为主要正确性证据。
- 已明确保持的不变量：
  - 不新增锁字段。
  - 未锁定态继续通过空白占位保持标题对齐。
  - 深色模式不新增专门的锁图标主题体系。

## 6. 附录：证据索引

| 证据ID | 代码位置 | 摘要 |
| ------ | -------- | ---- |
| E1 | `src/shared/types/index.ts:14` | `Note` 契约已定义锁字段 |
| E2 | `src/shared/types/index.ts:75` | 缺失锁字段会被标准化为未锁定 |
| E3 | `src/widgets/note-list/ui/note-list-item.vue:160` | note 行已具备固定宽度前导槽位 |
| E4 | `src/widgets/note-list/ui/note-list-item.vue:267` | 状态槽位颜色已接入 `var(--c-icon)` |
| E5 | `src/widgets/note-list/ui/note-list-item.vue:277` | 锁图标尺寸已独立收口 |
| E6 | `src/css/var.scss:47` | 浅色主题已提供图标色 token |
| E7 | `src/css/var.scss:53` | 深色主题通过 `ion-palette-dark` 切换 token |
| E8 | `tests/unit/components/note-list-item-lock-indicator.spec.ts:75` | 组件单测已覆盖锁住态 |
| E9 | `tests/integration/home/note-list-lock-indicator.spec.ts:137` | 集成测试已覆盖混合列表 |
